<?php

namespace {{ Namespace }}\Providers;

/**
 * {{ Name }} AI Provider.
 *
 * Implements a TitanCore provider contract so that all AI execution is routed
 * through TitanCoreModelGateway. Never call the upstream API directly from
 * application code — always go through the gateway.
 */
class {{ Name }}Provider
{
    public function __construct(
        private readonly array $config = [],
    ) {}

    /**
     * Execute a chat completion request through this provider.
     *
     * @param  array{
     *     model?: string,
     *     messages?: list<array{role:string,content:string}>,
     *     temperature?: float|int,
     *     max_tokens?: int
     * }  $payload
     * @return array{
     *     content: string,
     *     model: string,
     *     usage: array{prompt_tokens:int,completion_tokens:int,total_tokens:int}
     * }
     */
    public function chat(array $payload): array
    {
        // TODO: implement {{ Name }} provider chat completion
        throw new \RuntimeException('{{ Name }}Provider::chat() not implemented.');
    }

    /**
     * Check that the provider endpoint is reachable and the credentials valid.
     */
    public function healthCheck(): bool
    {
        // TODO: implement health check
        return false;
    }
}
