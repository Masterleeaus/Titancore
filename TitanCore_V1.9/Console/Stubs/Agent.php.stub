<?php

namespace {{ Namespace }}\Agents;

/**
 * {{ Name }} Titan Agent.
 *
 * Agents orchestrate tool execution and memory access through TitanCore.
 * They must never call AI providers directly.
 */
class {{ Name }}Agent
{
    /**
     * Run the agent with the given context.
     *
     * @param  array<string, mixed>  $context
     * @return array<string, mixed>
     */
    public function run(array $context): array
    {
        // TODO: implement {{ Name }} agent logic
        return [
            'agent'  => '{{ name }}',
            'status' => 'ok',
        ];
    }
}
